<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="cn.huangheNews">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="黄河新闻网"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.Design.Light.NoActionBar">
        <activity android:name=".activity.NewsContentActivity">
        </activity>
        <activity android:name=".activity.TypeContentActivity">
        </activity>
        <activity android:name=".activity.AllTypeActivity">
        </activity>
        <activity android:name=".activity.HotActivity">
        </activity>
        <activity android:name=".activity.LatestNews">
        </activity>
        <activity android:name=".activity.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>